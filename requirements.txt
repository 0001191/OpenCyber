# OpenCyber CTF Agent 依赖
# 安装: pip install -r requirements.txt

# 核心依赖
pyelftools>=0.31
pwntools>=4.12.0

# 可选 — 符号执行与约束求解
# angr>=9.2          # 符号执行
# z3-solver>=4.13    # 约束求解

# 可选 — 数据展示
# streamlit>=1.35     # Web 展示页
# gradio>=4.0         # 快速界面

# 可选 — LLM API 调用
# openai>=1.0         # OpenAI 兼容接口（DeepSeek/GLM/Qwen 等）
